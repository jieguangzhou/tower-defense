# Tower Defense MVP

前端代码位于 `frontend/`。

## 启动方式（npm）

1) 进入前端目录：

```bash
cd frontend
```

2) 启动本地静态服务器：

```bash
npm start
```

3) 在浏览器打开：

```
http://localhost:30000
```

> 说明：首次运行 `npm start` 会通过 npx 拉取 `http-server`，直接用 `file://` 打开会因为浏览器的模块加载限制而无法运行。

## 测试

```bash
cd frontend
npm test
```
